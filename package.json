{
  "name": "deckjump",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deck plans to reviewable material takeoffs",
  "engines": { "node": ">=22" },
  "scripts": { "start": "node server.mjs", "test": "node --test", "check": "node --check src/app.js && node --check src/takeoff.js && node --check server.mjs" }
}
